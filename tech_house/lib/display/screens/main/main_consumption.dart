import 'package:flutter/material.dart';

class MainConsuptiom extends StatelessWidget {
  const MainConsuptiom({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("puta hola"),
    );
  }
}
